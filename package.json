{
  "name": "mannen-fietsplanner",
  "version": "1.0.6",
  "private": true,
  "description": "Wind and rain forecast planner for cycling using Open-Meteo.",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/src && cp index.html dist/ && cp -R src/. dist/src/ && cp -R public/. dist/"
  }
}
