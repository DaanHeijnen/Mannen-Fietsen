{
  "name": "mannen-fietsplanner",
  "version": "1.1.0",
  "private": true,
  "description": "Cycling weather planner with GPX route uploads using Netlify Blobs.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "echo \"Static app: no build step required\" && mkdir -p dist && cp -R index.html src public netlify.toml README.md dist/",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "5.4.11"
  },
  "dependencies": {
    "@netlify/blobs": "latest",
    "@netlify/identity": "latest"
  }
}
