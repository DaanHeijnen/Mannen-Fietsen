{
  "name": "cyclecast-noord",
  "version": "1.0.6",
  "private": true,
  "description": "Windy-like cycling weather planner for the northern Netherlands using Open-Meteo.",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/src && cp index.html dist/ && cp -R src/. dist/src/ && cp -R public/. dist/"
  }
}
